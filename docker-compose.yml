version: '3.3'

services:
  db:
    image: postgres
  dms:
    build:
      context: ./dms
      args:
        mode: dev
    command: python3 manage.py runserver 0.0.0.0:80
    environment:
      - DJANGO_SETTINGS_MODULE=global.settings.dev
    volumes:
      - ./dms:/code
    ports:
      - "80:80"
    depends_on:
      - db
  ssl-proxy:
    build: ./ssl-dev
    ports:
      - "443:443"

